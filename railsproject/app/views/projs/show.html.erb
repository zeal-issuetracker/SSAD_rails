
<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @proj.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @proj.description %>
</p>

<p>
  <strong>Owner_id:</strong>
  <%= @proj.owner %>
</p>

<p>
  <strong>Settings:</strong>
  <%= @proj.settings %>
</p>

<p>
  <strong>Members:</strong>
  <%= @proj.users.each do |e| %>
    <%= e.email %>
    <% end%>
</p>

 <% if @proj.owner==current_user.id %>
    <%= link_to 'Edit', edit_proj_path(@proj) %> 
  <% end%>
<%= link_to 'Back', projs_path %>
<%= link_to 'add issue',{:controller => "issues" , :action => "new", :pro_id => @proj.id  } %>